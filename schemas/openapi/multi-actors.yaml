openapi: 3.0.3
info:
  title: Multi-Actor API
  description: |
    Dapr Actor API specification for multiple actor types.
    
    This specification demonstrates:
    - **CounterActor**: Simple state-based counter operations (Dapr StateManager)
    - **BankAccountActor**: Event-sourced bank account with transaction history (Dapr StateManager)
    - **WalletActor**: Event-sourced wallet using external event store (go-simple-eventstore)
    
    **Design Patterns**: This shows the contrast between:
    - State-based actors (CounterActor) - stores current state only
    - Event-sourced actors (BankAccountActor) - stores events and reconstructs state via Dapr
    - External event store actors (WalletActor) - stores events using third-party event store
    
    **Generated from**: This specification serves as the schema-first definition
    for generating Go interfaces and types for multiple actor types.
  version: 1.0.0
  contact:
    name: Dapr Actor Experiment
    url: https://github.com/shogotsuneto/dapr-actor-experiment
  license:
    name: MIT

servers:
  - url: http://localhost:3500/v1.0/actors
    description: Local Dapr sidecar (default configuration)

paths:
  # Counter paths
  /Counter/{actorId}/method/Get:
    get:
      summary: Get current counter value
      description: |
        Retrieves the current value of the counter for this actor instance.
        Uses state-based persistence - only current value is stored.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Current counter value
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounterState'

  /Counter/{actorId}/method/Increment:
    post:
      summary: Increment counter by 1
      description: |
        Increases the counter value by 1 and returns the new value.
        State-based operation - overwrites previous value.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Counter incremented successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounterState'

  /Counter/{actorId}/method/Decrement:
    post:
      summary: Decrement counter by 1
      description: |
        Decreases the counter value by 1 and returns the new value.
        State-based operation - overwrites previous value.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Counter decremented successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounterState'

  /Counter/{actorId}/method/Set:
    post:
      summary: Set counter to specific value
      description: |
        Sets the counter to a specific value provided in the request body.
        State-based operation - completely replaces current value.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetValueRequest'
      responses:
        '200':
          description: Counter value set successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CounterState'

  # BankAccount paths  
  /BankAccount/{actorId}/method/CreateAccount:
    post:
      summary: Create new bank account
      description: |
        Creates a new bank account with initial details.
        Event-sourced operation - stores AccountCreated event.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAccountRequest'
      responses:
        '200':
          description: Account created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccountState'

  /BankAccount/{actorId}/method/Deposit:
    post:
      summary: Deposit money to account
      description: |
        Deposits money to the account.
        Event-sourced operation - stores MoneyDeposited event.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DepositRequest'
      responses:
        '200':
          description: Money deposited successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccountState'

  /BankAccount/{actorId}/method/Withdraw:
    post:
      summary: Withdraw money from account
      description: |
        Withdraws money from the account if sufficient balance exists.
        Event-sourced operation - stores MoneyWithdrawn event.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawRequest'
      responses:
        '200':
          description: Money withdrawn successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccountState'
        '400':
          description: Insufficient funds

  /BankAccount/{actorId}/method/GetBalance:
    get:
      summary: Get current account balance
      description: |
        Gets current account balance and details.
        Event-sourced - balance calculated from event history.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Current account state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BankAccountState'

  /BankAccount/{actorId}/method/GetHistory:
    get:
      summary: Get transaction history
      description: |
        Gets complete transaction history for the account.
        Shows the power of event sourcing - full audit trail.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Transaction history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionHistory'

  # Wallet paths - demonstrates external event store usage
  /Wallet/{actorId}/method/CreateWallet:
    post:
      summary: Create a new wallet
      description: |
        Creates a new wallet instance using external event store.
        Demonstrates how actors can use third-party libraries for persistence.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWalletRequest'
      responses:
        '200':
          description: Wallet created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletState'

  /Wallet/{actorId}/method/AddFunds:
    post:
      summary: Add funds to wallet
      description: |
        Adds funds to the wallet using external event store.
        Events are stored using go-simple-eventstore instead of Dapr StateManager.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddFundsRequest'
      responses:
        '200':
          description: Funds added successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletState'

  /Wallet/{actorId}/method/SpendFunds:
    post:
      summary: Spend funds from wallet
      description: |
        Spends funds from the wallet if sufficient balance exists.
        Uses external event store for event persistence.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpendFundsRequest'
      responses:
        '200':
          description: Funds spent successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletState'

  /Wallet/{actorId}/method/GetBalance:
    get:
      summary: Get current wallet balance
      description: |
        Gets current wallet balance computed from events in external event store.
        Demonstrates event replay from third-party storage.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Current wallet state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletState'

  /Wallet/{actorId}/method/GetTransactions:
    get:
      summary: Get wallet transaction history
      description: |
        Gets complete transaction history from external event store.
        Shows integration with third-party event store for audit trail.
      parameters:
        - $ref: '#/components/parameters/ActorId'
      responses:
        '200':
          description: Transaction history from external event store
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletTransactionHistory'

components:
  parameters:
    ActorId:
      name: actorId
      in: path
      required: true
      description: |
        Unique identifier for the actor instance. Each actor ID maintains
        independent state within its actor type.
      schema:
        type: string
        pattern: '^[a-zA-Z0-9_-]+$'
        minLength: 1
        maxLength: 50
      example: "account-123"

  schemas:
    # Common error structure for all responses
    Error:
      type: object
      description: Error information returned within 200 responses
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Error code identifying the type of error
          enum: 
            - "VALIDATION_ERROR"
            - "AUTHENTICATION_ERROR"
            - "AUTHORIZATION_ERROR"
            - "INSUFFICIENT_FUNDS"
            - "ACCOUNT_NOT_FOUND"
            - "ACCOUNT_ALREADY_EXISTS"
            - "VALUE_OUT_OF_RANGE"
            - "INTERNAL_ERROR"
          example: "INSUFFICIENT_FUNDS"
        message:
          type: string
          description: Human-readable error message
          example: "Insufficient funds: balance 100.00, requested 150.00"
        details:
          type: object
          description: Additional error-specific details
          additionalProperties: true
          example:
            currentBalance: 100.00
            requestedAmount: 150.00
      additionalProperties: false
    # CounterActor schemas
    CounterState:
      type: object
      description: Current state of the counter actor (state-based)
      required:
        - success
      properties:
        success:
          type: boolean
          description: Whether the operation was successful
          example: true
        data:
          $ref: '#/components/schemas/CounterStateData'
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false

    CounterStateData:
      type: object
      description: Counter data (only present if success=true)
      required:
        - value
      properties:
        value:
          type: integer
          format: int32
          description: The current counter value
          example: 42
      additionalProperties: false

    SetValueRequest:
      type: object
      description: Request to set the counter to a specific value
      required:
        - value
      properties:
        value:
          type: integer
          format: int32
          description: The value to set the counter to
          minimum: -2147483648
          maximum: 2147483647
          example: 100
      additionalProperties: false

    # BankAccountActor schemas
    BankAccountState:
      type: object
      description: Current state of bank account (computed from events)
      required:
        - success
      properties:
        success:
          type: boolean
          description: Whether the operation was successful
          example: true
        data:
          $ref: '#/components/schemas/BankAccountStateData'
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false

    BankAccountStateData:
      type: object
      description: Account data (only present if success=true)
      required:
        - accountId
        - ownerName
        - ownerId
        - balance
        - isActive
        - createdAt
      properties:
        accountId:
          type: string
          description: Unique account identifier
          example: "account-123"
        ownerName:
          type: string
          description: Account owner name
          example: "John Doe"
        ownerId:
          type: string
          description: Account owner ID (for authorization)
          example: "user-123"
        balance:
          type: number
          format: double
          description: Current account balance (computed from events)
          example: 1250.50
        isActive:
          type: boolean
          description: Whether account is active
          example: true
        createdAt:
          type: string
          format: date-time
          description: Account creation timestamp
          example: "2024-01-15T10:30:00Z"
      additionalProperties: false

    CreateAccountRequest:
      type: object
      description: Request to create a new bank account
      required:
        - ownerName
        - initialDeposit
      properties:
        ownerName:
          type: string
          description: Name of the account owner
          minLength: 1
          maxLength: 100
          example: "John Doe"
        initialDeposit:
          type: number
          format: double
          description: Initial deposit amount
          minimum: 0
          example: 100.00
      additionalProperties: false

    DepositRequest:
      type: object
      description: Request to deposit money
      required:
        - amount
        - description
      properties:
        amount:
          type: number
          format: double
          description: Amount to deposit
          minimum: 0.01
          example: 250.00
        description:
          type: string
          description: Description of the deposit
          maxLength: 200
          example: "Salary deposit"
      additionalProperties: false

    WithdrawRequest:
      type: object
      description: Request to withdraw money
      required:
        - amount
        - description
      properties:
        amount:
          type: number
          format: double
          description: Amount to withdraw
          minimum: 0.01
          example: 50.00
        description:
          type: string
          description: Description of the withdrawal
          maxLength: 200
          example: "ATM withdrawal"
      additionalProperties: false

    TransactionHistory:
      type: object
      description: Complete transaction history (event sourcing benefit)
      required:
        - success
      properties:
        success:
          type: boolean
          description: Whether the operation was successful
          example: true
        data:
          $ref: '#/components/schemas/TransactionHistoryData'
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false

    TransactionHistoryData:
      type: object
      description: Transaction history data (only present if success=true)
      required:
        - accountId
        - events
      properties:
        accountId:
          type: string
          description: Account identifier
          example: "account-123"
        events:
          type: array
          description: List of all events in chronological order
          items:
            $ref: '#/components/schemas/AccountEvent'
      additionalProperties: false

    AccountEvent:
      type: object
      description: A single account event
      required:
        - eventId
        - eventType
        - timestamp
        - data
      properties:
        eventId:
          type: string
          description: Unique event identifier
          example: "evt-001"
        eventType:
          type: string
          description: Type of event
          enum: ["AccountCreated", "MoneyDeposited", "MoneyWithdrawn"]
          example: "MoneyDeposited"
        timestamp:
          type: string
          format: date-time
          description: When the event occurred
          example: "2024-01-15T10:30:00Z"
        data:
          type: object
          description: Event-specific data
          additionalProperties: true
          example:
            amount: 250.00
            description: "Salary deposit"
      additionalProperties: false

    # Wallet Actor schemas - external event store demonstration
    CreateWalletRequest:
      type: object
      description: Request to create a new wallet using external event store
      required:
        - ownerName
        - currency
      properties:
        ownerName:
          type: string
          description: Name of the wallet owner
          maxLength: 100
          example: "John Doe"
        currency:
          type: string
          description: Currency code for the wallet
          pattern: '^[A-Z]{3}$'
          example: "USD"
        initialBalance:
          type: number
          format: double
          description: Initial balance for the wallet
          minimum: 0
          default: 0
          example: 100.00
      additionalProperties: false

    AddFundsRequest:
      type: object
      description: Request to add funds to wallet
      required:
        - amount
        - description
      properties:
        amount:
          type: number
          format: double
          description: Amount to add
          minimum: 0.01
          example: 50.00
        description:
          type: string
          description: Description of the fund addition
          maxLength: 200
          example: "Monthly allowance"
      additionalProperties: false

    SpendFundsRequest:
      type: object
      description: Request to spend funds from wallet
      required:
        - amount
        - description
      properties:
        amount:
          type: number
          format: double
          description: Amount to spend
          minimum: 0.01
          example: 25.00
        description:
          type: string
          description: Description of the spending
          maxLength: 200
          example: "Coffee purchase"
      additionalProperties: false

    WalletState:
      type: object
      description: Current state of the wallet (from external event store)
      required:
        - success
      properties:
        success:
          type: boolean
          description: Whether the operation was successful
          example: true
        data:
          $ref: '#/components/schemas/WalletStateData'
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false

    WalletStateData:
      type: object
      description: Wallet state data (only present if success=true)
      required:
        - walletId
        - ownerName
        - currency
        - balance
        - isActive
        - createdAt
      properties:
        walletId:
          type: string
          description: Wallet identifier
          example: "wallet-123"
        ownerName:
          type: string
          description: Name of the wallet owner
          example: "John Doe"
        currency:
          type: string
          description: Currency code
          example: "USD"
        balance:
          type: number
          format: double
          description: Current balance
          example: 125.50
        isActive:
          type: boolean
          description: Whether the wallet is active
          example: true
        createdAt:
          type: string
          format: date-time
          description: When the wallet was created
          example: "2024-01-15T10:30:00Z"
      additionalProperties: false

    WalletTransactionHistory:
      type: object
      description: Complete transaction history from external event store
      required:
        - success
      properties:
        success:
          type: boolean
          description: Whether the operation was successful
          example: true
        data:
          $ref: '#/components/schemas/WalletTransactionHistoryData'
        error:
          $ref: '#/components/schemas/Error'
      additionalProperties: false

    WalletTransactionHistoryData:
      type: object
      description: Transaction history data from external event store
      required:
        - walletId
        - events
      properties:
        walletId:
          type: string
          description: Wallet identifier
          example: "wallet-123"
        events:
          type: array
          description: List of all wallet events in chronological order
          items:
            $ref: '#/components/schemas/WalletEvent'
      additionalProperties: false

    WalletEvent:
      type: object
      description: A single wallet event from external event store
      required:
        - eventId
        - eventType
        - timestamp
        - data
      properties:
        eventId:
          type: string
          description: Unique event identifier
          example: "evt-wallet-001"
        eventType:
          type: string
          description: Type of wallet event
          enum: ["WalletCreated", "FundsAdded", "FundsSpent"]
          example: "FundsAdded"
        timestamp:
          type: string
          format: date-time
          description: When the event occurred
          example: "2024-01-15T10:30:00Z"
        data:
          type: object
          description: Event-specific data
          additionalProperties: true
          example:
            amount: 50.00
            description: "Monthly allowance"
      additionalProperties: false